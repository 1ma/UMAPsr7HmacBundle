security:
    providers:
        hmac_provider:
            id: provider.test_user

    firewalls:
        default:
            pattern: ^/
            hmac:
                apikey_header: 'X-Api-Key'
            entry_point: testapp.entry_point
            provider: hmac_provider
            stateless: true
