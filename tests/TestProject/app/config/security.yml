security:
    providers:
        hmac_provider:
            id: provider.test_user

    firewalls:
        default:
            pattern: ^/
            hmac: ~
            provider: hmac_provider
            stateless: true
